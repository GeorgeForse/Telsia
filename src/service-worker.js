// This service worker is intentionally left empty.
// The core functionality is handled by the content script (content.js),
// which is injected directly into web pages.

// This file remains for potential future background tasks,
// such as managing extension-wide state or handling events
// that are not tied to a specific page's lifecycle.
